<UserControl
    x:Class="Domojee.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    d:DesignHeight="48"
    d:DesignWidth="400">

    <Grid Background="#FF004455">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <SymbolIcon Grid.Row="1" Symbol="{x:Bind Icon, Mode=OneWay}" Foreground="White" RenderTransformOrigin="0.5,0.5" >
                <SymbolIcon.RenderTransform>
                    <CompositeTransform ScaleY="0.5" ScaleX="0.5"/>
                </SymbolIcon.RenderTransform>
            </SymbolIcon>
            <TextBlock Grid.Row="0" Text="{x:Bind Title, Mode=OneWay}" FontWeight="Bold"/>
        </Grid>
    </Grid>
</UserControl>
