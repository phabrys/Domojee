<UserControl
    x:Class="Domojee.Views.Splash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Media="using:Microsoft.Xaml.Interactions.Media"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:jvm="using:Jeedom"
    xmlns:local="using:Domojee.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Jeedom"
    mc:Ignorable="d">

    <Grid Background="{StaticResource BlueBase}">
        <Image x:Name="image" HorizontalAlignment="Center" Margin="0,0,0,100" VerticalAlignment="Center" Source="ms-appx:///Assets/Square150x150Logo.png" Width="150" Height="150" />
        <TextBlock
            Margin="0,0,0,-100"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="21.333"
            FontWeight="Bold"
            Text="{Binding LoadingMessage, FallbackValue='Connexion en cours'}" />
        <ProgressBar Width="300" Value="{Binding Progress, FallbackValue=0}" Foreground="White" Margin="0,0,0,-200" />
    </Grid>
</UserControl>