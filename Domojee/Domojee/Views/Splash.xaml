<UserControl
    x:Class="Domojee.Views.Splash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Media="using:Microsoft.Xaml.Interactions.Media"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:jvm="using:Jeedom"
    xmlns:local="using:Domojee.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Jeedom"
    mc:Ignorable="d">

    <Grid Background="{StaticResource BlueBase}">

        <TextBlock
            Margin="0,0,0,100"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="21.333"
            FontWeight="Bold"
            Text="{Binding LoadingMessage, FallbackValue='Connexion en cours'}" />
        <ProgressBar Width="300" Value="{Binding Progress, FallbackValue=0}" />
        <ProgressRing
            x:Name="progressRing"
            Width="50"
            Height="50"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="White"
            IsActive="True" />
    </Grid>
</UserControl>