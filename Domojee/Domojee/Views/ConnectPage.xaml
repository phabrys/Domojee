<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using:Domojee.ViewModels"
    x:Class="Domojee.Views.ConnectPage"
    mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Name="Logo" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)" Storyboard.TargetName="image">
                <EasingDoubleKeyFrame KeyTime="0:0:2" Value="-1">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <CubicEase EasingMode="EaseIn"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
                <EasingDoubleKeyFrame KeyTime="0:0:4" Value="1">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <CubicEase EasingMode="EaseInOut"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)" Storyboard.TargetName="image">
                <EasingDoubleKeyFrame KeyTime="0:0:2" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:4" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>
    <Page.DataContext>
        <ViewModels:ConfigurationViewModel />
    </Page.DataContext>
    <Page.Background>
        <StaticResource ResourceKey="BlueBase"/>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="102"/>
            <RowDefinition Height="58"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <ContentThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock x:Name="tbTitle" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Domojee" Style="{StaticResource TitleTextBlockStyle}" Foreground="White"/>
            <TextBlock x:Name="tbSubtitle" HorizontalAlignment="Center" x:Uid="connection" Margin="0,5,0,0" TextWrapping="Wrap" Text="Connectez-vous à votre box domotique" Style="{StaticResource CaptionTextBlockStyle}" Foreground="White" RenderTransformOrigin="0.5,0.5"/>
            <Image x:Name="image" HorizontalAlignment="Center" Height="44" Margin="0,5,0,0" Width="44" Source="ms-appx:///Assets/Square44x44Logo.png" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <CompositeTransform/>
                </Image.RenderTransform>
            </Image>
        </StackPanel>
        <StackPanel x:Name="FlyoutURL" CornerRadius="2" BorderThickness="1" Padding="10,2" Margin="0,5" VerticalAlignment="Bottom" HorizontalAlignment="Center" Grid.Row="1" Background="White" RequestedTheme="Light">
            <StackPanel.Transitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition/>
                </TransitionCollection>
            </StackPanel.Transitions>
            <TextBlock Text="Entrez ici l'URL d'accès à votre box" HorizontalAlignment="Center"/>
            <TextBlock Text="Ex:  http://192.168.1.10/jeedom" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel x:Name="stackPanel" HorizontalAlignment="Center" VerticalAlignment="Top" CornerRadius="2" Padding="15,5,15,10" Width="280" Grid.Row="2" BorderThickness="1" Background="White" RequestedTheme="Light">
            <TextBox x:Name="tbAddress" Margin="0,0,0,5" TextWrapping="Wrap" VerticalAlignment="Top" PlaceholderText="URL d'accès au serveur" Header="Serveur" GotFocus="tbAddress_GotFocus" LostFocus="tbAddress_LostFocus" Text="{Binding Address, Mode=TwoWay}"/>
            <TextBox x:Name="pbAPIKey" Margin="0,5" VerticalAlignment="Top" PlaceholderText="Clé API à récupérer dans JEEDOM" Header="Clé API" Text="{Binding ApiKey, Mode=TwoWay}"/>
            <Button x:Name="bConnect"  Margin="0,5,0,0" Content="Connexion" Click="bConnect_Click" HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</Page>
