<Page
    x:Class="Domojee.Views.ScenePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ctl="using:Domojee.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush Stretch="UniformToFill" ImageSource="ms-appx:///Images/WP.jpg"/>
    </Page.Background>
    <ctl:MainMenu x:Name="menu">
        <ScrollViewer Tag="Scénarios">
            <StackPanel>
                <Grid Background="{StaticResource BlueBase}" Height="48">
                    <TextBlock Text="Scénarios" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <GridView x:Name="gridview" Margin="15" ItemTemplate="{StaticResource SceneTemplate}" ItemClick="gridview_ItemClick" IsItemClickEnabled="True" ItemsSource="{x:Bind SceneList}" SelectionMode="None"/>
            </StackPanel>
        </ScrollViewer>
    </ctl:MainMenu>
</Page>
