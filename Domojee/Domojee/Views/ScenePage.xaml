<Page x:Class="Domojee.Views.ScenePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ctl="using:Domojee.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Domojee.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Images/WP.jpg" Stretch="UniformToFill" />
    </Page.Background>
    <ctl:MainMenu x:Name="menu">
        <ScrollViewer Tag="Scénarios">
            <StackPanel>
                <Grid Height="48" Background="{StaticResource BlueBase}">
                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="18"
                               FontWeight="Bold"
                               Text="Scénarios" />
                </Grid>
                <GridView x:Name="gridview"
                          Margin="15"
                          IsItemClickEnabled="True"
                          ItemClick="gridview_ItemClick"
                          ItemTemplate="{StaticResource SceneTemplate}"
                          ItemsSource="{x:Bind SceneList}"
                          SelectionMode="None" />
            </StackPanel>
        </ScrollViewer>
    </ctl:MainMenu>
</Page>
