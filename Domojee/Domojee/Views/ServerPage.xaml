<Page
    x:Class="Domojee.Views.ServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctl="using:Domojee.Controls"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush Stretch="UniformToFill" ImageSource="ms-appx:///Images/WP.jpg"/>
    </Page.Background>

    <ctl:MainMenu x:Name="menu">
        <ScrollViewer Tag="Serveur">
            <StackPanel>
                <Grid Background="{StaticResource BlueBase}" Height="48">
                    <TextBlock Text="Serveur JEEDOM" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Text="Actions sur le serveur" FontSize="20" HorizontalAlignment="Center"/>
                <GridView Margin="15" HorizontalAlignment="Center" SelectionMode="None" IsItemClickEnabled="True" ItemClick="GridView_ItemClick">
                    <GridViewItem Width="115" Height="60" Margin="10">
                        <Grid Tag="update">
                            <Rectangle Width="115" Height="60" Opacity="0.5" RadiusX="2" RadiusY="2" Fill="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            <TextBlock Text="Mettre à jour" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </GridViewItem>
                    <GridViewItem Width="115" Height="60" Margin="10">
                        <Grid Tag="reboot" VerticalAlignment="Center">
                            <Rectangle Width="115" Height="60" Opacity="0.5" RadiusX="2" RadiusY="2" Fill="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            <TextBlock Text="Redémarrer" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </GridViewItem>
                    <GridViewItem Width="115" Height="60" Margin="10">
                        <Grid Tag="shutdown"  VerticalAlignment="Center">
                            <Rectangle Width="115" Height="60" Opacity="0.5" RadiusX="2" RadiusY="2" Fill="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            <TextBlock Text="Eteindre" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </GridViewItem>
                </GridView>
                <TextBlock x:Name="messageText" Text="Liste des messages" FontSize="20" HorizontalAlignment="Center"/>
                <ListView x:Name="messageView" ItemsSource="{x:Bind MessageList}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </ctl:MainMenu>
</Page>
