<Page x:Class="Domojee.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ctl="using:Domojee.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Domojee.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:selector="using:Domojee.Selectors"
      xmlns:viewModels="using:BackgroundWorker"
      mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Images/WP.jpg" Stretch="UniformToFill" />
    </Page.Background>
    <Page.Resources>
        <DataTemplate x:Key="ObjectTemplate">
            <Grid CornerRadius="2"
                  Holding="Grid_Holding"
                  RightTapped="Grid_RightTapped">
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Click="MenuFlyoutItem_Click"
                                        Tag="{Binding id}"
                                        Text="Modifier l'image" />
                        <MenuFlyoutItem Click="MenuFlyoutItem_Click_1"
                                        Tag="{Binding id}"
                                        Text="Réinitialiser l'image" />
                        <MenuFlyoutItem Click="MenuFlyoutItem_Click_Epingler"
                                        Tag="{Binding id}"
                                        Text="Epingler" />
                    </MenuFlyout>
                </FlyoutBase.AttachedFlyout>

                <Image Width="192"
                       Height="108"
                       Source="{Binding Image}"
                       Stretch="UniformToFill" />
                <Rectangle Width="192"
                           Height="48"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Fill="{StaticResource BlueBase}"
                           Opacity="0.8" />
                <StackPanel VerticalAlignment="Bottom">
                    <TextBlock x:Name="txtName"
                               HorizontalAlignment="Center"
                               FontSize="18"
                               FontWeight="Bold"
                               Text="{Binding Name,
                                              FallbackValue=Name}"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="Wrap" />
                    <TextBlock x:Name="txtCount"
                               Margin="0,0,5,5"
                               HorizontalAlignment="Right"
                               FontSize="10"
                               Text="{Binding Count,
                                              FallbackValue=0}" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <ctl:MainMenu x:Name="menu">
        <Grid>
            <ProgressRing x:Name="progressUpdating"
                          Width="50"
                          Height="50"
                          Canvas.ZIndex="2"
                          IsActive="{x:Bind Updating, FallbackValue=False}" />
            <StackPanel>
                <Grid Height="48" Background="{StaticResource BlueBase}">
                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="18"
                               FontWeight="Bold"
                               Text="Tableau de Bord" />
                </Grid>
                <TextBlock HorizontalAlignment="Center" Text="Sélectionner un objet" />
                <ScrollViewer>
                    <GridView x:Name="objectview"
                              Margin="15"
                              IsItemClickEnabled="True"
                              ItemClick="objectview_ItemClick"
                              ItemTemplate="{StaticResource ObjectTemplate}"
                              ItemsSource="{x:Bind ObjectList}" />
                </ScrollViewer>
            </StackPanel>
        </Grid>
    </ctl:MainMenu>
</Page>