<Page
    x:Class="Domojee.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Views"
    xmlns:viewModels="using:Domojee.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctl="using:Domojee.Controls"
    xmlns:selector="using:Domojee.Selectors"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Images/WP.jpg" Stretch="UniformToFill"/>
    </Page.Background>
    <Page.Resources>
        <DataTemplate x:Key="ObjectTemplate">
            <Grid CornerRadius="2" RightTapped="Grid_RightTapped">
                <Image Width="192" Height="108" Stretch="UniformToFill" Source="ms-appx:///Images/WP.jpg" />
                <Rectangle Width="192" Height="48" Fill="{StaticResource BlueBase}" Opacity="0.8" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
                <StackPanel VerticalAlignment="Bottom">
                    <TextBlock x:Name="txtName" TextWrapping="Wrap" Text="{Binding Name, FallbackValue=Name}" TextTrimming="CharacterEllipsis" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtCount" Text="{Binding Count, FallbackValue=0}" HorizontalAlignment="Right" Margin="0,0,5,5" FontSize="10"/>
                </StackPanel>
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Tag="{Binding id}" Text="Changer l'image" Click="MenuFlyoutItem_Click"/>
                    </MenuFlyout>
                </FlyoutBase.AttachedFlyout>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <ctl:MainMenu x:Name="menu">
        <Grid>
            <ProgressRing x:Name="progressUpdating" Canvas.ZIndex="2" Width="50" Height="50" IsActive="{x:Bind Updating, FallbackValue=False}"/>
            <StackPanel>
                <Grid Background="{StaticResource BlueBase}" Height="48">
                    <TextBlock Text="Tableau de Bord" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Text="Sélectionner un objet" HorizontalAlignment="Center"/>
                <ScrollViewer>
                    <GridView x:Name="objectview" ItemsSource="{x:Bind ObjectList}" Margin="15"  SelectionMode="None" IsItemClickEnabled="True" ItemClick="objectview_ItemClick" ItemTemplate="{StaticResource ObjectTemplate}"/>
                </ScrollViewer>
            </StackPanel>
        </Grid>
    </ctl:MainMenu>
</Page>
