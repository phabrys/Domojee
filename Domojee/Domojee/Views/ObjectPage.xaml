<Page
    x:Class="Domojee.Views.ObjectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:selector="using:Domojee.Selectors"
    xmlns:converter="using:Domojee.Converters"
    xmlns:ctl="using:Domojee.Controls"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Images/WP.jpg" Stretch="UniformToFill"/>
    </Page.Background>
    <Page.Resources>
        <converter:EqLogicStateConverter x:Key="EqLogicStateConverter"/>
        <converter:EqLogicUpdatingConverter x:Key="EqLogicUpdatingConverter"/>
        <converter:EqLogicNotUpdatingConverter x:Key="EqLogicNotUpdatingConverter"/>
        <DataTemplate x:Key="EqLogicTemplate">
            <Grid>
                <Rectangle Width="150" Height="100" RadiusX="2" RadiusY="2" Fill="{StaticResource SystemControlForegroundAccentBrush}" Opacity="0.5"/>
                <TextBlock Text="{Binding name, FallbackValue=Name}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding eqType_name, FallbackValue=Type}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5" FontSize="8"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ZWaveEqLogicTemplate">
            <RelativePanel>
                <Rectangle Width="300" Height="100" RadiusX="2" RadiusY="2" Fill="LawnGreen" Opacity="0.5" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True"/>
                <TextBlock x:Name="txtType" Text="openzwave" FontSize="8" RelativePanel.AlignTopWithPanel="True" Margin="5" RelativePanel.AlignRightWithPanel="True" />
                <TextBlock x:Name="txtName" Text="{Binding name, FallbackValue=Name}" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="txtType" FontWeight="Bold" FontSize="18"/>
                <StackPanel x:Name="stkTools" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="txtName" Orientation="Horizontal">
                    <Button x:Name="btnON" Margin="5,0" Tag="{Binding id}" FontFamily="Segoe MDL2 Assets" Click="btnON_Click">
                        <Grid Width="20" Height="20">
                            <ProgressRing Visibility="{Binding Updating, Converter={StaticResource EqLogicUpdatingConverter}}" IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding State, Converter={StaticResource EqLogicStateConverter}}" Visibility="{Binding Updating, Converter={StaticResource EqLogicNotUpdatingConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{Binding State, Converter={StaticResource EqLogicState2BrushConverter}}"/>
                        </Grid>
                    </Button>
                    <StackPanel Width="100" Margin="0,0,5,0">
                        <TextBlock Text="Puissance"  HorizontalAlignment="Center" FontSize="12"/>
                        <TextBlock Text="{Binding Puissance, FallbackValue=N/A}" HorizontalAlignment="Center" FontSize="10"/>
                    </StackPanel>
                    <StackPanel Width="100" Margin="0,0,5,0">
                        <TextBlock Text="Consommation" HorizontalAlignment="Center" FontSize="12"/>
                        <TextBlock Text="{Binding Consommation, FallbackValue=N/A}" HorizontalAlignment="Center" FontSize="10"/>
                    </StackPanel>
                </StackPanel>
            </RelativePanel>
        </DataTemplate>
        <selector:EqLogicTemplateSelector x:Key="EqLogicTemplateSelector" EqLogicTemplate="{StaticResource EqLogicTemplate}" ZWaveEqLogicTemplate="{StaticResource ZWaveEqLogicTemplate}"/>
    </Page.Resources>
    <Grid Background="{StaticResource BlackBack}">
        <ProgressRing x:Name="progressUpdating" Canvas.ZIndex="2" Width="50" Height="50" IsActive="{x:Bind Updating, FallbackValue=False}"/>
        <ScrollViewer>
            <StackPanel>
                <Grid Background="{StaticResource BlueBase}" Height="48">
                    <TextBlock Text="{x:Bind ObjectName}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <ctl:VariableSizedGridView x:Name="eqlogicview" ItemsSource="{x:Bind EqLogicList}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="eqlogicview_ItemClick" ItemTemplateSelector="{StaticResource EqLogicTemplateSelector}" Padding="15">
                    <ctl:VariableSizedGridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VariableSizedWrapGrid ItemHeight="100" ItemWidth="150" Orientation="Horizontal" MaximumRowsOrColumns="5"/>
                        </ItemsPanelTemplate>
                    </ctl:VariableSizedGridView.ItemsPanel>
                </ctl:VariableSizedGridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
