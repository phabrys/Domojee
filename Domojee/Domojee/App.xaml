<Application
    x:Class="Domojee.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Domojee"
    RequestedTheme="Dark">
    <Application.Resources>
        <VisualStateGroup x:Key="menuVSG">
            <VisualState x:Name="None">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="0"/>
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="splitView.(SplitView.IsPaneOpen)" Value="False"/>
                    <Setter Target="splitView.(SplitView.DisplayMode)" Value="Overlay"/>
                </VisualState.Setters>
            </VisualState>
            <VisualState x:Name="Compact">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="960"/>
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="splitView.(SplitView.IsPaneOpen)" Value="False"/>
                    <Setter Target="splitView.(SplitView.DisplayMode)" Value="CompactInline"/>
                </VisualState.Setters>
            </VisualState>
            <VisualState x:Name="Wide">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="1280"/>
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="splitView.(SplitView.DisplayMode)" Value="CompactInline"/>
                    <Setter Target="splitView.(SplitView.IsPaneOpen)" Value="True"/>
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <SolidColorBrush x:Key="BlueBase" Color="#FF0088AA"/>
        <SolidColorBrush x:Key="Blue10" Color="#FF19BBB2"/>
        <SolidColorBrush x:Key="Blue20" Color="#FF33A0BB"/>
        <SolidColorBrush x:Key="Blue20Alpha" Color="#AA4c81ad"/>
        <SolidColorBrush x:Key="Blue30" Color="#FF4CABC3"/>
        <SolidColorBrush x:Key="Blue40" Color="#FF66B8CC"/>
        <SolidColorBrush x:Key="BlackBack" Color="#7F000000"/>
        <DataTemplate x:Key="SceneTemplate">
            <Grid Margin="10" Width="120">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Rectangle Width="120" Height="70" RadiusX="2" RadiusY="2" Fill="{StaticResource SystemControlForegroundAccentBrush}" Opacity="0.5" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
                <TextBlock Text="{Binding Name, FallbackValue=Name}" HorizontalAlignment="Center" Grid.ColumnSpan="2" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding LastLaunch, FallbackValue=none}" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="1" FontSize="10" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ProgressRing IsActive="{Binding Updating, FallbackValue=False}" Grid.Row="1" Margin="5,0,0,0" />
            </Grid>
        </DataTemplate>
    </Application.Resources>
</Application>
